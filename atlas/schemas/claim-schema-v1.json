{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Private Capital Atlas Claim Schema v1",
  "type": "object",

  "required": [
    "id",
    "subject",
    "predicate",
    "status",
    "confidence"
  ],

  "properties": {
    "id": {
      "type": "string",
      "pattern": "^claim:[a-z0-9]+(?:-[a-z0-9]+)*$"
    },

    "subject": {
      "type": "string",
      "pattern": "^[a-z]+:[a-z0-9]+(?:-[a-z0-9]+)*$"
    },

    "predicate": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_]*$"
    },

    "object": {
      "type": "string",
      "pattern": "^[a-z]+:[a-z0-9]+(?:-[a-z0-9]+)*$"
    },

    "value": {
      "$ref": "./value-schema-v1.json"
    },

    "temporal": {
      "type": "object",

      "properties": {
        "start": {
          "type": [
            "string",
            "null"
          ]
        },

        "end": {
          "type": [
            "string",
            "null"
          ]
        },

        "precision": {
          "type": "string"
        },

        "status": {
          "type": "string"
        }
      },

      "additionalProperties": false
    },

    "role": {
      "type": "string"
    },

    "context": {
      "type": "object",
      "additionalProperties": true
    },

    "status": {
      "type": "string",
      "enum": [
        "VERIFIED",
        "SUPPORTED",
        "REPORTED",
        "DISPUTED"
      ]
    },

    "confidence": {
      "type": "string",
      "enum": [
        "HIGH",
        "MEDIUM",
        "LOW",
        "UNKNOWN"
      ]
    },

    "evidenceRefs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },

  "oneOf": [
    {
      "required": [
        "object"
      ],

      "not": {
        "required": [
          "value"
        ]
      }
    },

    {
      "required": [
        "value"
      ],

      "not": {
        "required": [
          "object"
        ]
      }
    }
  ],

  "additionalProperties": false
}
